[build]
  command = "npm install && node build.js && rm -rf node_modules && cd 'YVALAW OS' && npm install && npm run build && mkdir -p ../os && cp -r dist/. ../os/"
  publish = "."
  functions = "YVALAW OS/netlify/functions"

[functions."run-autopay"]
  schedule = "0 13 * * *"
